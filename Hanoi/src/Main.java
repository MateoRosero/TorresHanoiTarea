import javax.swing.*;

public class Main {
    public static void main(String[] args) {
        Interfaz app = new Interfaz();
        app.setSize(890, 472);
        app.setVisible(true);
        app.setTitle("Interfaz");
        app.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}